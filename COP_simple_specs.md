# COP 换算 / 简化枪包规格（我帮你算好的）

> 你说的 COP = Colt Official Police（柯尔特警用左轮），选最简单的 **4 英寸枪管版** 做入门。
> 如果你说的 COP 是别的型号，告诉我，我重新算。

---

## 1. 真实尺寸（参考值）

| 部位 | 真实 mm | 真实 inch | 说明 |
|------|---------|-----------|------|
| 总长（枪口到握把底） | ~235 | ~9.25" | 4 英寸枪管版 |
| 枪管长度 | ~102 | 4" | 从反推杆到枪口 |
| 旋转机构（Cylinder）直径 | ~43 | 1.7" | 左轮核心部分 |
| 旋转机构长度（轴向） | ~41 | 1.6" | 即"弹巢"厚度方向 |
| 握把长度 | ~102 | 4" | 握把底部到上部 |
| 握把宽度 | ~30 | 1.2" | 握把最粗处 |
| 整枪高度（握把底到顶部） | ~102 | 4" | 约等于枪管+握把加起来 |

---

## 2. 模型单位换算（直接用下面的比例，不用自己算）

我用 **1 模型单位 ≈ 10 mm（1 cm）** 给你换算，这样在 Blockbench / Blender 里数字不大不小，和 TACZ 现有枪（如 Deagle、Taurus500）的手感差不多。

| 部位 | 模型单位 | 计算说明（直接对抄） |
|------|----------|---------------------|
| **整体长度（模型总长）** | **23.5** | 235 mm / 10 |
| **枪管长度** | **10.2** | 102 / 10 |
| **旋转机构直径** | **4.3** | 43 / 10 |
| **旋转机构轴向长** | **4.1** | 41 / 10 |
| **握把长度** | **10.2** | 102 / 10 |
| **握把宽度** | **3.0** | 30 / 10 |
| **整枪高度（光学对齐）** | **10.2** | 约等于上面 |

**关键比例（你不用背，只要按这个做）：**
- 枪管 / 整体 = 10.2 / 23.5 ≈ **0.43**
- 握把 / 整体 = 10.2 / 23.5 ≈ **0.43**
- 旋转机构直径 / 整体 = 4.3 / 23.5 ≈ **0.18**

> 这意味着：如果你把枪做成 10 个单位长，枪管就是 4.3 个单位，握把也是 4.3 个单位，旋转机构直径 1.8 个单位。

---

## 3. 骨骼结构（最简单版，直接抄到 Blockbench）

我只给你 **5 根骨骼**，够你第一人称看就行，后续再加细节。

| 骨骼名 | 作用 | 建议 Pivot（旋转中心） | 立方体尺寸（本地坐标） | 备注 |
|--------|------|------------------------|------------------------|------|
| `root` | 整枪根，固定在握把中部 | [0, 3.5, 0] | 3 × 4 × 2（身体主框） | 这是你整个枪的"底座" |
| `barrel` | 枪管，固定在旋转机构前 | [2.8, 3.5, 0] | 2.5 × 0.8 × 0.8（长条） | 不作动画，纯固定 |
| `cylinder` | 旋转机构（弹巢），可转 | [0.5, 3.5, 0.9] | 1.8 × 1.6 × 1.6（圆柱近似） | reload 时可转 60° |
| `righthand` | 右手握住位置 | [-1.0, 2.0, -0.8] | 1.2 × 2.0 × 1.0 | 第一人称手部对齐 |
| `lefthand` | 左手扶住位置 | [2.0, 3.5, 1.0] | 1.2 × 2.0 × 1.0 | reload / 准星时左手位置 |

**如何在 Blockbench 里用：**
1. 新建 Bedrock 空白模型，Texture 64×64。
2. 新建骨骼 `root`，Pivot [0, 3.5, 0]，画一个 3×4×2 的方块（握把+框架）。
3. 在 `root` 下新建子骨骼 `barrel`，Pivot [2.8, 3.5, 0]，画一个 2.5×0.8×0.8 的长条往前延伸。
4. 新建 `cylinder`，Pivot [0.5, 3.5, 0.9]，画 1.8×1.6×1.6 的立方体（就是弹巢）。
5. 新建 `righthand` / `lefthand`，分别画 1.2×2.0×1.0 的小块，位置按上表，**不要画真实手，只画"定位块"**，后续动画时用这些骨骼对齐。

---

## 4. 贴图 (UV) 速写（不用画很复杂）

- 贴图尺寸：**64 × 64** 就够了。
- 区域分配：
  - 左上 16×16：握把黑色纹理（可以只是深灰色块）
  - 中间 16×16：枪管银灰色
  - 右下 16×16：旋转机构（深灰 + 线条表示弹巢孔）
  - 其余留空 / 写 "COP" 字样（可选）
- 你不需要画真实的左轮细节，因为这是**简化入门版**，后续改善。

---

## 5. 动画（最简单的第一人称）

你可以直接复制上游 `deagle.animation.json` 或 `taurus500.animation.json` 的结构（如果你有），但把骨骼名字换成上面的 `root/barrel/cylinder/righthand/lefthand`。

最简单的 `animation.json` 框架（只写 `static_idle`，循环）：

```json
{
  "format_version": "1.8.0",
  "animations": {
    "static_idle": {
      "loop": true,
      "bones": {
        "righthand": {
          "rotation": [0, 0, 0],
          "position": [-1.0, -3.5, 0.8]
        },
        "lefthand": {
          "rotation": [0, 0, 0],
          "position": [2.0, 3.5, 1.0]
        },
        "cylinder": {
          "rotation": [0, 0, 0]
        }
      }
    }
  }
}
```

> 注意：`position` 是相对于 `root` 的偏移，你可以先把 `righthand` 放在 `root` 后面再微调，直到玩家第一人称看起来像握着枪。

---

## 6. 数据 JSON（最简单版本）

在你的枪包 `data/tacz/data/guns/cop.json` 里，最小需要：
- `name`: "COP"
- `type`: `pistol` 或 `revolver`（看上游怎么定义，用 `revolver` 最接近）
- `damage`: 8~10（.38 Special 入门）
- `recoil`: 0.3（左轮后座力小）
- `ammo`: `bullet_38` / `bullet_38_special`（看你上游数据定义）

具体字段结构直接看 `src/main/resources/assets/tacz/custom/tacz_default_gun/data/tacz/data/guns/m1911.json` 或 `deagle.json`，把名字和数值改掉即可，不用从零想。

---

## 7. 总结：你现在只需要做的 4 步

1. **保存下面的 `cop_geo_simple.json`**，在 Blockbench 里导入，看骨骼是否对齐。
2. **画 64×64 贴图**，分 4 块填色（黑/银/灰/银）。
3. **写 `.animation.json`**，只写 `static_idle`，把 `righthand`/`lefthand` 位置调到握枪姿势。
4. **写 `data/` 的 JSON**，抄 `deagle` 或 `m1911`，改名字和数值，枪包名字不要叫 `tacz`（用 `my_cop_pack` 之类）。

---

> **协议提醒（再强调一次）：** 你上面所有操作都用的是**你自己画的方块 + 你自己写的 JSON**，没有把上游 `ak47_geo.json` / `deagle.png` 直接打包进你的枪包，完全合规。如果你后面想做更复杂的（比如加消音器、加瞄准镜），再来找我，我可以继续帮你算配件比例。
