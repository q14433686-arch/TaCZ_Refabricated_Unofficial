accessWidener v2 official

# Confirmed against the Minecraft 26.1.2 development jar by compileJava.
# Keep entries minimal; the remaining 1.21.1 targets must be individually revalidated.
accessible field net/minecraft/world/entity/LivingEntity jumping Z
accessible method net/minecraft/client/multiplayer/MultiPlayerGameMode ensureHasSentCarriedItem ()V
accessible method net/minecraft/client/Minecraft startUseItem ()V
# Scope depth cleanup brackets a delegated RenderType draw with glDepthRange(1, 1).
accessible method net/minecraft/client/renderer/rendertype/RenderType <init> (Ljava/lang/String;Lnet/minecraft/client/renderer/rendertype/RenderSetup;)V
# LRTactical 近战暴击判定：Player#canCriticalAttack 在 26.1.2 是【包级私有】
# （字节码确认 access flags 无 public/protected）。它封装了原版全部暴击条件
# （fallDistance>0 && !onGround && !onClimbable && !isInWater
#  && !isMobilityRestricted && !isPassenger && target instanceof LivingEntity && !isSprinting），
# 直接复用可保证与原版【完全一致】而非行为近似，故开放访问而不是手写一遍条件。
accessible method net/minecraft/world/entity/player/Player canCriticalAttack (Lnet/minecraft/world/entity/Entity;)Z
