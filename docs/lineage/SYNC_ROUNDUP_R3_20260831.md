# R3 定稿轮 · 全家族同步总纲（2026-08-31）

> **本文是定稿快照 + 同步任务的主次排序**，写于 26.2(main) R3 发版收口时。
> 四条姊妹线的进度以本文档发稿时刻**逐分支拉取远端核实**为准（非猜测）。
> 旧的三份 08-30 同步指导（`SYNC_GUIDE_REFAB_1211/2612/RENOV_262`）**仍然有效**，
> 本文不重复其机制细节，只做三件事：①刷新进度底账；②标注哪些段落已过时；
> ③给出 R3 之后的移植主次。账本（`HANDOFF_LEDGER.md`）状态行同步更新。
>
> 维护者裁定（2026-08-31）：**这批移植没有争议**——1.21.11 的 VBO 已实机跑通，
> 各分支按本文顺序推进即可，无需再逐项请示。

---

## 0. 四线进度底账（2026-08-31 远端实拉核实）

| 分支 | 仓库 | 尖端（非 ci-log） | 状态一句话 |
|---|---|---|---|
| **26.2(main) 工作线**（本分支 `01a04e96`） | refab | `bb6fcb6` | **R3 定稿收口中**：TML 第 0-3 步全实装；世界 GPU 两轮实机返修完（世界消费点/法线弹栈）；姊妹审查 A1-A10 处置完（7 采纳 2 驳回 1 半）；光影下世界枪照明是**唯一待实测项** |
| **1.21.11 线**（`01a05759`） | refab | `ab11a84` | **R3 已定稿在前**：第 0/1/2/3 步全实机 PASS（含世界 VBO + 光影组合）；光影两键与绕序开关经实机 A/B 后默认全关；已发布自己的 26.2 审查文档与「1211→2612」同步指导 |
| **26.1.2 线**（`01a05170`） | refab | `c0362ea` | **等待位**：final-overlay 回搬完（`9a4e71f`）+ 动画两连修完（`19d22b8`+`a36ed59`）+ CI 已补（`034ac77`/`c0362ea`）——08-30 指导的第一波**已全部完成**；TML 整线为零，正等 1211 定稿后按其指导整包搬 |
| **renov 26.2 主线**（`01a04ea3`） | renov | `e4c4623` | **文档同步位**：第三轮同步核对（R3）已记录、TML 第 1 步实机状态已回填；meshloader 代码未搬（一事一议中） |
| **renov 26.1.2**（`01a051b1`） | renov | `caeb9e2` | final-overlay 同步完，待实测并入；后续跟 renov 主线节奏 |

**共识事实（多线交叉验证过，可当公理引用）**：
- 上游 TML 的 poly_mesh **绕序与镜像本就自洽**——「镜像必须反转绕序」的推论
  被 1211 实机否证（entityCutout 剔背面 ⇒ 反转后整枪近乎全黑，Forge 原版对照确认）。
  两仓的 `MeshPolyMirrorReverseWinding` 均已定为**默认 false**（诊断项）。
- `IlluminatedRealSky`（发光件天空光）机制两仓都实装了，但 1211 的 A/B 把
  「继承日月亮度」追到了别的根因 ⇒ 两仓均**默认 false**（诊断项）。
- 「退化面不写零法线」是这轮法线追查里**唯一无争议净收益**，两仓都保留。
- 26.2 与 1.21.11 的 GPU 层**物理不可互抄**（26.2 有 `RenderType.prepare()`/
  `drawFromBuffer`/`PreparedFrame#executeSolid`，1211 没有；反向同理）——
  跨线搬的是**机制与判定**，不是代码。

---

## 1. R3 之后的移植主次（按依赖与收益排序）

### 主线一（最高优先）：26.1.2 接收 TML 整线
- **执行者**：`01a05170`。**货源**：1211 分支的
  `docs/lineage/SYNC_GUIDE_1211_TO_2612_TML_GPU_20260831.md`（它们已写好，
  含 20 文件终态清单 + 陷阱注记）——**以那份为准**，26.1.2 与 1211 同纪元
  （SubmitNodeCollector 世代、无 prepare()），从 1211 拿而不是从我们拿。
- **从我们 26.2 侧补拿的三件**（1211 指导也这么写了，对表确认）：
  光照档 LRU、逐出延迟释放、每帧烘焙额度——但**额度/容量解耦后的版本**
  （`MeshGpuBakeBudgetPerFrame` 独立键）以两仓 R3 终态为准。
- 前置已全部就位（final-overlay/动画修/CI 均 DONE），**可以立刻开工**。

### 主线二：renov 26.2（04ea3）的 meshloader 一事一议收口
- 08-30 指导 §3 的「一事一议」现在有了定稿答案：两仓 R3 终态都验过机制，
  renov 侧若要 TML，按其 NeoForge 表皮重排我们的 26.2 版（同纪元），
  重点搬 §2 的审查处置后终态（分表禁用/不回写配置/LinkageError/override 防御）。
- 04ea3 侧 PIP 终态对表（08-30 指导 §1）**仍然 OPEN**，优先级高于 meshloader
  ——那是防「PR#82 泄漏 bug 在他们那边潜伏」的对表，涉及已发布内容。

### 主线三：两条 26.1.2（refab 05170 / renov 051b1）的实测并入
- 各自的 final-overlay 回搬都停在「待实测」。实测通过后并入各自长期分支,
  账本 #1/#2 行关账。

### 收尾件（无依赖，见缝插针）
- 账本 #4（掩码顺序无关性核对）与 #6（08-12 老 handoff 清账）：下次触碰
  对应代码时顺手关账，不单独立项。
- refab 1.21.11 的 rerender 移植（账本 #5）维持 **DECLINED**，不重启。

---

## 2. 工作流/CI 同步（代码之外的部分，本轮新增）

R3 期间 26.2 工作线沉淀了三个 workflow + 一套暂存区惯例，各线按需采用：

| 件 | 26.2 状态 | 各线建议 |
|---|---|---|
| `compile-check.yml` v4（主分支+PR 也编译；仅 arena push 回推日志；concurrency 省配额） | **已上线** | 1211 线已有同族（`56c03d6` 等 ci-log 证明在跑）；**26.1.2 已补**（`c0362ea`）；renov 两线各自有回推日志在跑,可对照 v4 补 concurrency 与主分支触发 |
| `build.yml`（三静态校验：mixin 类存在性/中英键齐平/版本一致性 + 全量 build + jar artifact 14 天） | **已上线**（artifact 名不能含 `/` 的坑已修） | **建议全家族采用**——jar artifact 是「发链接即可征测」的基础设施，NV 卡征测、姊妹互测都靠它;三静态校验把「mixin 拼错类名」「语言键漏翻」从实机问题降级为 CI 红 |
| `consistency.yml` v2（版本号五处一致守门,arena/** 也触发） | **已上线** | 有 README 版本号纪律的线（refab 全family）建议跟；renov 侧看其 README 约定自行裁量 |
| `docs/ci/` 暂存区惯例（agent 无 workflow 权限 ⇒ 写暂存区 + 维护者网页端上线,`docs/ci/README.md` 记上线状态表） | 运转中 | 各线遇到 workflow 改动照此办理,不要让 agent 直接碰 `.github/workflows/` |
| 文档目录制（`docs/` 根=现行 / `investigations/`=日期型完结 / `lineage/`=同步唯一入口 / `publish/`） | 已落地 | 1211 线已自发形成同构结构;其余线在下次文档清理时对齐,不急 |

---

## 3. 对四份既有同步文档的时效标注

| 文档 | 时效 | 说明 |
|---|---|---|
| `SYNC_GUIDE_REFAB_2612_20260830.md` | **第一波已全部完成**（CI/05170 并入前置/动画修）,第二、三波仍有效 | 第二波的取货点按 1211 新指导修正:TML 从 1211 拿,深度 PIP 仍等 052b2 收口（DECLINED 不影响 26.1.2 侧接收既有 Step-3 成果的计划,以 052b2 处置为准） |
| `SYNC_GUIDE_RENOV_262_20260830.md` | §1 PIP 对表仍 OPEN（最高优先）;§3 meshloader 一事一议有了定稿输入 | 动画两连修一项:04ea3 侧是否已修**未核实到 commit 级**,对表时一并查 |
| `SYNC_GUIDE_REFAB_1211_20260830.md` | §0 收尾（TEMP 剥离/Step 合并）**已被 1211 的 R3 定稿实质完成** | 该线已反超本指导的预期进度,后续以 1211 自己的文档为准 |
| 1211 侧 `SYNC_GUIDE_1211_TO_2612_TML_GPU_20260831.md` | **现行有效,26.1.2 接收 TML 的唯一货源文档** | 其中「26.2 有三件比 1211 好,从 26.2 拿」的清单与本文 §1 主线一互为印证 |

---

## 4. R3 发版检查衔接

发版流程照 `docs/publish/RELEASE_CHECKLIST.md` §0-§5 走。与本文相关的两点：
- §2 冒烟里 mesh 部分按 `MESH_LOADER.md` §5.2-bis 矩阵,其中第 6 项
  （光影下世界枪照明）为**已知待实测**——不阻塞发版（`MeshGpuWorld`
  游戏内可关,失败半径已分表隔离）,但 Release 说明里如实标注;
- Release 征测点沿用既定的 NV 四项 + 新增「光影下地上/别人手里的 mesh 枪
  亮度是否随世界光照走」。
