# 跨分支/跨仓 Handoff 登记账本（refab **1.21.11** 分支副本）

> 用法见 **26.2 分支上的** `docs/lineage/SYNC_GOVERNANCE_PROPOSAL.md` §4-§5（那份以及下面几份
> `SYNC_GUIDE_*` 只存在于 `arena/01a04e96`，本分支没有副本 —— 读法：
> `gh api -H "Accept: application/vnd.github.raw" "repos/q14433686-arch/TaCZ_Refabricated_Unofficial/contents/<路径>?ref=arena/01a04e96-tacz-refabricated-unofficial"`）。状态：
> `OPEN`（待认领）/ `CLAIMED(分支)`（进行中）/ `DONE(commit)` / `DECLINED(原因)` / `需核对`。
> 本文件是 `arena/01a04e96`（26.2）上同名账本的**分支副本**：各行状态由本分支回填，
> 上游那份不动；两边不一致时，**涉及 1.21.11 的行以本副本为准**，其余以正式账本为准。
> 本副本首刊 2026-08-31（R3 轮）。

## 1. 上游账本里指向本分支的行（核对结果）

| # | handoff / 计划文档 | 方向 | 状态 | 本分支核对（2026-08-31） |
|---|---|---|---|---|
| 7 | **26.2 上的** `docs/lineage/SYNC_GUIDE_REFAB_1211_20260830.md`（1.21.11 暂停收尾 + 可同步项） | 26.2 → 1.21.11 | **DONE**（本轮收口，见下） | §0.1 TEMP 剥离 → 探针已固化成 `scripts/mesh_render_probe.gradle`（默认不接入，`-PmeshProbe` 显式开），`build.gradle` 里不再有任何 TEMP 块；§0.2 `docs/SCOPE_PIP_RERENDER_1211_PORT_PLAN_20260830.md` 顶部 `STATUS: DECLINED` 已在；§0.3 `SCOPE_PIP_RERENDER` 默认 false 已在；§1.1 检视动画两连修（`4aa8d7b`+`12d6f3c`）**已在**（`AnimationStateContext#stopAnimation` 的出生序号判据、`ObjectAnimationRunner.SPAWN_COUNTER`、`AnimationStateMachine#trigger` 的栈式快照三处逐一核到，无需搬）；§1.2 的结论被本分支推翻，见 §2 |
| 5 | `docs/SCOPE_PIP_RERENDER_1211_PORT_PLAN_20260830.md` | 26.2(main) → 1.21.11 | **DECLINED**（维护者 2026-08-30 裁定） | 本分支已带 `STATUS: DECLINED` 头；半成品按裁定 (a) 保留、默认锁 false |
| 8 | **26.2 上的** `docs/lineage/SYNC_GUIDE_REFAB_2612_20260830.md` 第 4 步「等 1.21.11 收口」 | 26.1.2 ← 本分支 | **READY** | 本分支 R3 已把 TML 第 0/1/2/3 步连同实机结论与注入点事实整理成 `SYNC_GUIDE_1211_TO_2612_TML_GPU_20260831.md`，26.1.2 可以按它开工 |

## 2. 本分支新开的行

| # | 事项 | 方向 | 状态 | 备注 |
|---|---|---|---|---|
| L-1 | `docs/lineage/SYNC_GUIDE_1211_TO_2612_TML_GPU_20260831.md`：内置 TML 第 0/1/2/3 步整包 + 六条不变量 + 26.1.2 需先实测/先决定的 Q1-Q10 | 1.21.11 → 26.1.2（`arena/01a05170`） | **OPEN（他们已按指导移植并提交，实测缺口见 L-9）** | 26.1.2 目前 meshloader 文件数为 0（`git ls-tree` 核实），所以是「先第 0 步、再往上叠」，不是打 GPU 补丁。要求回给：Q1-Q10 的答案（Q7/Q8 你们能静态读到、比我们有优势）+ 世界路径两条主验收项的实机结论 + Q10 你们选哪个选项 **2026-08-31 追加**：§1.4（光影下两键与 `MeshPolyIlluminatedRealSky` 的默认值别照抄成 true）、 §1.6（**一个必须你们自己决定的未修 bug**：poly 绕序 × 背面剔除，本分支只做了规避）、§2 扩到六条不变量、 §3 扩到十件事（新增 Q8 剔除状态 / Q9 枪包绕序约定的点积统计 / Q10 你们的选法）、§5.3/§5.5 两条验收 补「挡天体不继承自发光」与「法线矩阵已回包」。核过的你们进度：`1.1.8+fabric.26.1.2.R2-hotfix2`、 `compat/meshloader/` 404、`docs/` 无 `lineage/`、活动停在 08-30（`compile-check-2612.yml` + 动画两连修）。**2026-09-01 追加**：他们已提交移植（`9ed6b938`…`79a6391`，`compat/meshloader/` 现有 20 个 java）⇒ 上面「文件数为 0」已过期，指导 §0/§1.6 已加过期横幅；Q8/Q10 已回、Q9 双方都未测，答复收录在 `SYNC_REVIEW_2612_TML_PORT_20260901.md` §7 |
| L-2 | `docs/REVIEW_UPSTREAM_TML_GPU_262_20260831.md`：对 26.2 GPU 层的静态审查（A1-A9） | 1.21.11 → 26.2（`arena/01a04e96`） | **OPEN** | 只读审查、无运行期断言。A1（渲染目标硬绑）与 A2（异常时关总闸 + 回写配置）建议优先；A4 需要他们实机对表才能定性 |
| L-3 | 上游 §1.2「GPU 烘焙层不可搬到 1.21.11（硬依赖 26.2 的 `RenderPipeline.builder`/`BindGroupLayouts`）」 | 26.2 → 1.21.11 | **OVERTURNED** | 1.21.11 上确有 `RenderPipeline.builder` 路线（本分支两条路都在用），第 1/2/3 步已实机 PASS；「不可搬」的说法请上游从指导文档里改掉。真正不可搬的是**方向相反**的三样：`PreparedFrame#executeSolid`、`RenderType#prepare()`、`PreparedRenderType#drawFromBuffer`（1.21.11 CI javap 核实不存在） |
| L-4 | CI 对齐：`compile-check` v4（主分支/PR 触发 + concurrency + 只在 arena/** 回推日志） | 26.2 → 1.21.11 | **待维护者粘贴** | 三份都只到了 `docs/ci/`：Agent 凭据推不动 `.github/workflows/`（GitHub App 无 `workflows` 权限，push 被远端整体拒绝，实测 2026-08-31）。26.2 侧若已知这条限制，请把 `docs/ci/README.md` 的措辞同步成「三份全是暂存件」 |
| L-7 | `_illuminated` 骨骼恒烘 (block=15, sky=15) ⇒ 光影包里「遮不住太阳/月亮」：本分支加了 `MeshPolyIlluminatedRealSky`（仅光影下换真实 sky） | 1.21.11 → 26.2 与 26.1.2 | **OPEN（只改 poly 层，待实机）** | 数字在三个分支的 `PolyMeshModel` 与 `BedrockPart#render` 里逐字相同 ⇒ 同源；本分支刻意只覆盖 poly 层，因为立方体层影响所有枪包与所有准星点、配置该归 `ClientConfig`。**建议 26.2 一次把两层收进同一个键**（见 `REVIEW_UPSTREAM_TML_GPU_262_20260831.md` 的 A10 续集）。维护者实机前，这条对本分支也是「静态修复」；判别法在 `docs/MESH_LOADER.md` §5.8 末段。**同日更新（L-8）**：维护者报的现象不是这条，键已退回**默认 false**（代码与推导保留，见 §5.8 的定性块）；给 26.2 的「默认 true」建议一并作废 |
| L-6 | `core/PolyMesh.java` 的镜像绕序 / 枪包法线缺陷（A10）：`docs/MESH_LOADER.md` §5.7 的修复 + 三个配置开关 | 1.21.11 → 26.2（`arena/01a04e96`）**与** 26.1.2（`arena/01a05170`） | **PARTIAL（绕序那一半被实机否证并退回；法线两项保留）** | 上游 26.2 `587763c` 的该文件与本分支改前**逐字相同** ⇒ 同一缺陷；26.1.2 目前还没有 meshloader，等它整包取文件时会自动带上本修复，所以它那边要守的是「别把 `FORCE_FLAT_SHADING` 恢复成常量、别删 `normals` 解析」。本分支只做到静态 + CI 编译 ⇒ **随后维护者实机回包，把这一条推翻了一半**（同枪包同光影，拿 Forge 原版对照）： 「关掉绕序反转」那格才对 ⇒ `MeshPolyMirrorReverseWinding` 已退回默认 **false**（2026-08-31 晚）。 留下的两条与绕序无关：退化面不写零法线、`MeshPolyPreferPackNormals`。矩阵与结论见 §5.7（已回填）； **要闭合正反面不自洽，得先决定剔除策略**（`entityCutout` vs `entityCutoutNoCull`）或从数据反推绕序， 本分支两条都没做。26.2 那边请把「反转绕序」这条建议作废，只搬上面那两条 |
| L-8b | **未修 BUG 立项**：poly 的绕序与背面剔除相互抵消 ⇒ 光影下（走 collector 时）任何"只修一层"的做法都会让高模枪近乎全黑。触发器是 A10 那轮把 `MeshPolyMirrorReverseWinding` 默认开；维护者用同枪包、同光影包（**Complementary Shaders - Unbound**）与 Forge 原版并排对照，确认关掉才对 ⇒ 本分支已退回默认 false 并回填 §5.7 矩阵 | 1.21.11 → 26.2 **与** 26.1.2（`arena/01a05170`） | **DONE（规避，本分支）/ OPEN（不自洽本身，不修，已立项）** | 全文在 `docs/MESH_LOADER.md` **§6**：症状原话、环境与变量、四步复现（**第 ④ 步「有没有镂空」我们没做，是接手者的第一步**）、三层来源与"为什么会抵消"、三个候选修法与代价（① 换不剔背面的 render type＝行为改动、要整轮重验；② 从数据逐面反推绕序＝要一批真枪包统计；③ 维持现状＝本分支选的）。静态可证的只有一条：本仓 collector 的顶点数据与上游 `587763c` 逐字相同、只差发射绕序那一位 ⇒ 关掉即与原版等价；`entityCutout` 是否剔背面在沙箱核不了（无 Loom jar），只有实机对照这一条间接证据。**26.1.2 请自己决定，别照抄**（他们那边 `meshloader` 目录还不存在、26.1.2 的 pipeline 可静态读 ⇒ 见 SYNC_GUIDE §1.6 与 §3 Q8-Q10。**2026-09-01 更正**：「他们那边 meshloader 目录还不存在」已过期（他们移植了，且用字节码答了 Q8）；那条反证把我们这边的主解释改成「光影包按 `gl_FrontFacing` 取反法线是承重的」，「collector 剔背面」降级为未排除的次要分支，判别法换成「`MeshPolyInvertNormals=true` 与 `MeshPolyMirrorReverseWinding=true`（绕序原样）两格观感是否几乎相同」——两格都仍未跑。详见 `MESH_LOADER.md` §5.7/§6 与 L-9） **2026-09-01：本行不再用来解释「反光/高光偏一侧」** —— 那条已定案在**法线矩阵的读取时刻**（26.2 `83daf16`，我方同步见 L-15 与 `docs/MESH_LOADER.md` §6.1）；绕序 × 剔除的不自洽**本身仍未修**（近全黑风险、四步判别的第 ④ 格仍未跑） |
| L-9 | 对 26.1.2 那版 TML/GPU 移植的**只读复核**（`docs/SYNC_REVIEW_2612_TML_PORT_20260901.md`，按代码不按文档；基准 `79a6391`） | 1.21.11 → 26.1.2（`arena/01a05170`） | ****CLOSED（按他们 tip `7562abcb` 逐项独立核对：孤儿 mixin 配置、`.idea/.gradle` untrack、`SoundEngineMixin` 正式目标名、`LevelRendererAccessor` 已注册、`PapiManager` 与两处 tooltip 均已纯查表 ⇒ 我方 7 项请回全部关闭；只剩"光影下两个 GPU 键默认值相反"一条开放）**** | P0：① `FeatureRenderDispatcherMixin` 未注册 ⇒ 世界 GPU 表整条静默失效（`renderAtWorldFlush` 唯一调用者；`require = 0` 让它连 WARN 都没有）；② lang 曾被整文件覆盖（移植轮只剩 36 键 ⇒ 维护者看到的 `item.`/`tooltip.`），补回后仍缺 2 个被代码引用的键；③ 孤儿 `tacz.compat.acceleratedrendering.mixins.json` + 其 mixin 类不存在。P1：`latest.log`/`.idea`/`.gradle` 被 track、`lrtactical.mixins.json` 少 `client.SoundEngineMixin`、README 零提及 mesh、他们的 `docs/MESH_LOADER.md` 丢了「枪包怎么用」「弹匣链路」两节。同时收下他们的 Q8 字节码结论改写了本仓 §5.7/§6 的解释（见 §4） |
| L-10 | 镜内 `text_show`（MK5/MK5HD 弹药计数）**从不显示**：`functionalTasks` 在瞄具深度孔径路径上没被 flush。根因与修法由 26.1.2 给出，本分支已落地（三个文件） | 26.1.2 → 1.21.11（收）；回礼 2 条给 26.1.2（26.2 无此洞） | **代码已改，实机未验**（剧本见 `docs/lineage/SCOPE_TEXT_SHOW_1211_20260901.md` §4 四格；两条根因见 §5） | 静态闭合三条：`submitFunctionalTasks` 全仓仅 `BedrockModel.java:381` 一处且瞄具不走 `super.submit`；26.2 第 681 行确有 `super.submit` ⇒「26.2 没这洞」我方独立确认；本仓无 `ScopeTextSubmitter`/`scope_text.*sh` ⇒ 26.2 的 `9d036594` 是裁剪不是存在性。回礼差别①：他们把 `else` 分支的 flush 放在 `!bodySnapshot.isEmpty()` 门内 ⇒「只有文字没有本体几何」的快照仍会丢；差别②：`ocularRingSnapshot` 的任务他们没 flush。**延迟覆盖层那一格文字不测镜筒深度 ⇒ 贴边计数仍可能溢出，真要裁需移植 26.2 那套**（本分支有意不做）。**同日第二条根因**：`PapiManager.getTextShow` 用 `I18n.get`（1.21.11 的 javap 实测=查表后必跑 String.format，catch 成 "Format error: "+原文），MK5HD 的 `"%ammo_count%"` 因此被污染 ⇒ 已改 `Language.getInstance().getOrDefault`，与 26.2 的 `ec51f556` 同法；同形另两处 tooltip（`ClientAttachmentItemTooltip:165`、`ClientBlockItemTooltip:75`）一并改了 —— **这两处 26.2 至今没改**，26.1.2 三处都没改 ⇒ 已回给两边 |
| L-11 | 评估 26.1.2 本轮 PIP 回移植（`0a77ef52`…`8aca7374`）对本分支的可加项：A 窄遍后状态重提取/清提交节点、B 镜内文字掩码裁剪、C 隔帧 interval | 26.1.2 → 1.21.11（评估） | **A 已结案（不加，javap 依据已进 `ScopePipRerender` 注释）；**B 已落地并实机 PASS（2026-09-01 维护者报：无光影 A 格贴边被裁、F5 重载剧本 F 正常）**；C 排队** | 他们把我们的 PIP 线整条搬过去后修出两个实机回归（镜外实体/太阳/雾全灭 = `LevelRenderState` 一次性燃料被窄遍消费；镜内文字溢出圆孔 = 字体管线不吃孔径深度）。**A（结案）**：探针实测本世代 `LevelRenderer` 无 `extractLevel`（`extractVisibleEntities` 等私有且以 `LevelRenderState` 为入参）、`GameRenderer` 只有 `renderLevel(DeltaTracker)`+`private extractCamera(float)`、`renderAllFeatures()` 自身以 `submitNodeStorage.clear()` 收尾（另有我们自己 `FeatureRenderDispatcherMixin` 的字节码记录）⇒ 两步都不需要，"留给后续阶段"那句注释已改写为结论。**B**：`ScopeTextSubmitter`+`scope_text_final.fsh`+`ScopeRenderTypes.maskedText`+`TextShowRender.clipToScopeMask`（失败回退 vanilla，不丢字）——唯一能让剧本 A 格成立的做法。探针 v3 实测：`prepareText` 七参**同形**，但 `GlyphVisitor` 是 `acceptGlyph(TextRenderable$Styled)`/`acceptEffect(TextRenderable)` 两个方法（他们覆写的 `accept(r,x,y,w)` 在本世代不存在，原样搬编译不过），而 `TextRenderable` 自带 `textureView()`/`render(Matrix4f,VertexConsumer,int,boolean)` ⇒ 分组更简单；本世代 `shaders/core/` 下**没有** `rendertype_text.json`（管线由 Java 侧 `RenderSetup` 声明）。v4 在问 `RenderPipelines` 的 TEXT 常量名与 `RenderSetup`/`AbstractTexture` 能力面。**B 的落地清单已成型**（评估篇 §2.2）：`clonePipeline(RenderPipelines.TEXT)` + `withFragmentShader("core/scope_text_final")` + 我们既有的两条深度 uniform + `RenderSetup.withTexture` + `DepthCopyRenderType(Operation.MASK)` + `assignPipelineToIris("HAND_TRANSLUCENT")` ⇒ 新文件只多 1 个 fsh + 1 个 `ScopeTextSubmitter`。v5 已答：`RenderSetupBuilder` 只有 `withTexture(String, Identifier[, Supplier<GpuSampler>])` ⇒ 壳纹理是**必需**的（`AbstractTexture` 的 protected 字段可直接赋值）；`Font$DisplayMode` = NORMAL/SEE_THROUGH/POLYGON_OFFSET，只需 `TEXT` 一族。**三轮探针已收工并从 `build.gradle` 删除** ⇒ B 事实层面无阻塞，**已按维护者"动"的指示实现完**（新增 `ScopeTextSubmitter` + `scope_text_final.fsh`，`ScopeRenderTypes` 加 `MASKED_TEXT_PIPELINE`/`maskedText`， `ScopeDepthCopyState` 加 `isMaskCycleValid()`，`TextShowRender` 加 `clipToScopeMask`， 瞄具侧 true/枪身侧 false；掩码不可用回退 vanilla 不丢字）。施工清单与三处有意偏离见评估篇 §2.3； 验收判据（含新增的 log-once 与剧本 F「F5 重载后掩码缓存是否失效」）见镜内文字篇 §4。 状态：CI 编译绿（`d076cf5` → ci-log `8a2f399`）+ 维护者实机 PASS ⇒ 镜内文字裁剪结案；光影格 C/D 与 PIP 格 E 是否也在本轮 PASS 内，等维护者一句话再收口。**C**：键与代数守卫我们都没有，成本小但排在 A 之后（且 `SCOPE_PIP_RERENDER` 在我们这边是 DECLINED 默认关）。**不要回搬**：`ScopePipRenderState`/`ScopePipDepthDebug`/配置面（逐字段比对：他们 11 个 `SCOPE_PIP_*` 键、我们 10 个，唯一差异正是 C 那条 interval）/`DepthHandle` 快照都是我们自己的东西；他们 `3e4eeb16` 把光影下两键默认改回 ON 与我们 B 测结论相反，已回问其实测数据。详见 `docs/lineage/SYNC_REVIEW_2612_PIP_BACKPORT_20260901.md` |
| L-12 | 我方世界 GPU 消费点是否要从 `FeatureRenderDispatcher#renderAllFeatures` RETURN 挪到"世界 solid flush"：26.1.2 `99e505f6` 在他们那代证明挂 `renderAllFeatures` 会 (1) 世界表没人消费 ⇒ 存活证明失败、永久回落 collector，(2) 被 Iris 在 level 内的手部渲染以"手部 MV 槽"消费 ⇒ 几何贴在视空间；后者与我方仍未修的 L-8b「反光/高光偏一侧」症状高度相似（**2026-09-01 更正**：那条症状根因已定在法线矩阵读取时刻，见 L-15 ⇒ 它不再是"贴视空间"的旁证；四点位表照旧要） | 26.1.2 → 1.21.11（**待其四点位表**） | **OPEN（我方不动，先要判据）** | 我方现有的依据只有 `FeatureRenderDispatcherMixin` 类注释里那条"主通道一次 + 粒子节点一次 + 手部尾一次"的字节码记录，**没有** 26.1.2 那种四点位全表 ⇒ 若 1.21.11 主通道那次 `renderAllFeatures` 不是"世界 solid flush"，消费点就得挪。已在 `docs/lineage/SYNC_CHECKLIST_1211_TO_2612_PIP_20260901.md` §5 向他们索取：调用点全表 + 区分"贴视空间"与"绕序×剔除抵消"的两个开关组合与三条日志。**挪之前不下任何结论**，也不动 L-8b 的立项状态。 |
| L-13 | 兄弟分支那条 26.1.2 独有 BUG 的**世代边界**记录：`58831e4f`「FCAP `ForgeConfigSpec.save()` 恒为 no-op ⇒ Cloth 改的配置重启被重置」在 1.21.11 **不适用**（我方 FCAP `21.11.1` 的落盘链是带 autosave 的旧文件配置；他们自己文档 §0 也写了"1.21.11、26.2 都没有这个问题"） | 1.21.11 ← 26.1.2（判定：不跟） | **CLOSED（不跟，已记世代理由）** | 目的只有一个：防止后人把 `ConfigPersist.java` + `ConfigTrackerAccessor`/`ModConfigAccessor` 当"两分支对齐项"搬进 1.21.11 —— 我们没有那条断桥，搬过来只会多出两个无用的 mixin 与一条每帧落盘。同步写进 `docs/lineage/SYNC_CHECKLIST_1211_TO_2612_PIP_20260901.md` §1 末行，请他们在自己文档里保留世代边界标注。证据级别：静态（读双方 `TaCZFabric`/`*ClothConfig` 与 FCAP `21.11.1` vs `26.1.5`，并引其文档 §0 的三分支逐字比对）；我方未对 FCAP 21.11.1 做 javap、也未做重启实机 ⇒ 若要升到字节码级，补一轮探针即可（只影响"要不要预防性加 save"，不影响当前行为），未做我方实机重启验证。 ⇒ **2026-09-01 被维护者推翻**：1.21.11 同样中招（"搜不到 save() 调用"正是病本身，不是"无该病"的证据；他们文档那句"21.11.1 无此病"是版本推测、不可当我方结论）。修法见 L-21 |
| L-21 | 重启后配置回默认（`ConfigPersist` 显式落盘） | 26.1.2 `58831e4`+`0651171` → 1.21.11 | **已落地待实机重启复测**（CI `success`） | 根因：FCAP 新架构下 `ConfigValue.set()` 只写内存、`ForgeConfigSpec.save()` 因 `childConfig` 不是 `FileConfig` 而**静默 no-op** ⇒ 面板"保存"后无人写盘。落点：新建 `config/ConfigPersist`（注册时钉 `tacz-client/common.toml` + `saveAll()` 用 `TomlWriter` 显式写回）+ `mixin/client/ForgeConfigSpecAccessor`（取包私有 `childConfig`；`LoadedConfig.save()` 不可达）+ `MenuIntegration` 的 `setSavingRunnable`。比他们多一层 `instanceof FileConfig` 分岔 ⇒ 不必先证明本线走哪条架构、也不会写坏文件。**不覆盖 SERVER**（`/tacz config` 那三条属 SyncConfig，归 FCAP 的 serverconfigs 拷贝路径）；我先加在 ConfigCommand 的 `saveAll()` 已撤（假动作）。取代 L-13 的"非项"判定 |
| L-22 | 配置持久化的剩余出口：`tacz-pre.toml` 从未登记进 `ConfigPersist`（面板 `DefaultPackDebug` 改的就是它）+ `save()` 静默跳过 + 截断写 | 26.1.2 修法的覆盖面订正 → 1.21.11 | **已落地待实机复测**（CI `success`） | 新增 `ConfigPersist.recordNamed`；`PreLoadConfig` 注册时登记落盘；`OverwriteCommand` set 后 `saveAll()`；`childConfig == null` 由静默改 WARN；已存在文件走 `CommentedFileConfig` 合并 + `ATOMIC_MOVE`（原 `newBufferedWriter` 截断 = 半写即空文件 = 全线回默认）。26.1.2 同版本代码同病（其 `ConfigPersist` 只记 client/common、第 98 行静默 return、第 108 行截断写）⇒ 非搬错。上一则撤回的 `ConfigCommand.saveAll()` 不恢复（SERVER 键归 FCAP） |
| L-14 | 姊妹项目 NeoForge `1.21.11` 线的镜内 `text_show` 同步（对象经维护者更正后重核） | 1.21.11 → **TaCZ_Renovated@1.21.11**（判定：清单已发，对方开工） | **OPEN**：等对方回"§2/§3/§4 各落到哪个提交 + 实机结果"；我方**不改**对方项目任何文件、不替对方记 ✅ | 按对方 tip `e3d9dd5c` 逐文件读码实测：两个根因全在（`client/renderer/snapshot/BedrockRenderSnapshot.java:93` 的 `submitFunctionalTasks` 在 `BedrockAttachmentModel` 里无调用点；`client/model/papi/PapiManager#getTextShow` 用 `I18n.get`），且 `TextShowRender` 无掩码裁剪、无 `ScopeTextSubmitter`/`scope_text_final.fsh`、`ScopeDepthCopyState` 只有私有 `maskValid`。清单 = `docs/lineage/SYNC_CHECKLIST_1211_NEOFORGE_SISTER_20260901.md`（§5 明确别搬的：PIP 两键、FCAP、全部 TML/mesh GPU 项——对方 `meshloader`/`ScopePip` 均 0 命中）。我方欠一条：对方要 §4 完整 patch 即 `git show d076cf5 -- <三文件>` |
| L-15 | 光影下 GPU mesh 枪**反光/高光偏一侧**：法线矩阵的读取时刻（26.2 `83daf16` 的病灶，本分支同步修） | 1.21.11 ← 26.2（`83daf16`，经 26.1.2 线转达） | **已落地待实机**（`PolyMeshGpuRenderer#drawList` 每条绘制 pushMatrix / mul(entry.model()) / finally popMatrix） | 机制：光影包的 `gl_NormalMatrix` 由 Iris 在**绘制执行那一刻**读 RenderSystem MV 栈顶求逆转置（`ExtendedShader#iris$setupState`），**不吃** DynamicTransforms 快照；而本仓 GPU 顶点法线是骨骼本地系（`PolyMesh#writeRaw` 裸写 `setNormal`），旋转全靠这个矩阵补 ⇒ 栈顶无 pose 层就按本地法线打光。我方原形状比 26.2 首版更裸：**从未压栈**（只 pass 外 `writeTransform`、pass 内 `setUniform`）。位置不受影响（走快照）。vanilla 不受牵连：两条管线都带 `NO_CARDINAL_LIGHTING`，核心 entity shader 不读法线。1.21.11 的逐绘制触发点为 `GlCommandEncoder#executeDraw → trySetup`（审计见 `docs/RENDER_PIPELINE_SCOPE_AUDIT_26_1_2.md`）。**证据级别**：机制=26.2 实机实锤；我方=静态推导 + CI 编译门，**实机未验**；判别法与边界全文在 `docs/MESH_LOADER.md` §6.1。不加配置键（BUG 修复不做成可开关） |
| L-16 | GPU pass 体内触发纹理懒加载（`drawList` 在 pass 内 `resolveTextureView`）：全部件走 GPU 的高模包贴图错误 | 1.21.11 ← **26.1.2**（其 `2ae4c29`，经维护者转达；26.2 同病灶**未修**） | **已落地待实机**（视图整批在 `createRenderPass` 前解析进 `viewsByTexture`；失败日志改 per-texture log-once） | 机制：`TextureManager#getTexture` 对未加载纹理同步 `registerAndLoad → ReloadableTexture#apply → CommandEncoder#writeToTexture`，而 `writeToTexture` 在 pass 打开期间禁止 ⇒ 抛 `Close the existing render pass before performing additional commands`；被 `catch` 吞掉后回退 missing texture，且纹理永远没机会在 pass 外加载 ⇒ 逐帧炸、逐帧重试（维护者日志"连续三条"）。判别面：只有**没有任何 collector 兄弟部件**的包会踩（duyupack kar98un）。同批自查全仓另三处自建 pass 不碰 `TextureManager`。全文 `docs/MESH_LOADER.md` §5.11。**26.2 线请转告**（其 `resolveTextureView` 与我方逐字相同、仍在 pass 内） |
| L-17 | 26.1.2 渲染线整批移植（`7562abcb`→`5c45a787`，42 真提交逐条甄别） | 26.1.2 → 1.21.11（按其同步文本 + **其 commit 正文**双轨核对） | **已落地待实机**（G1 mesh 目镜裁剪、G2 掩码帧戳、G3 手部坐过窄遍、G4 PIP 阈值/渐变） | 判定全文 `docs/lineage/SYNC_REVIEW_2612_RENDER_LINE_20260901.md`。要点：只取 `3a62d00` 终态不取被推翻的 `752ee9e`；`b9f9db7` 否决；G5 光影隔离大件不移植（本线无 `compat/voxy`/`compat/sodium` 且 `ScopePipRerender:150` 仍 B1 硬拒）；他们把 `03a807e` 列为待搬**是错的**（我方 `c9b8ba1` 已做且更大）；`93178de`/`2ae4c29` 我方当日已各自等价落地。首轮 CI 红（冲突合并少一层右括号 + 日志重复）已由 `0fcb9512` 修。**我方一处刻意不同**：`ScopeTextSubmitter` 保留跨帧 `isMaskCycleValid()`，不得"顺手统一"成当帧闸（理由见甄别篇 §4 尾）。他们 §6 要我核的 5 点已逐条回答 |
| L-18 | 光影下二次渲染的时域隔离大件（`ScopePipIsolatePipeline`） | 26.1.2 G5（`3e8b22e`…`d3f0fdc`）→ 1.21.11 | **已落地待实机**（CI `success`） | 维护者裁定"可以有"⇒ 撤销 L-17 里"整组不移植"的结论。落地 `IrisScopePipelineCompat`（纯反射）+ `IrisScopeDimensionMixin` + `IrisShadowResolutionMixin` + 四键 + 预热/空闲释放；硬拒改成 `shaderIsolateSafe()`。**Sodium 通道同批搬进**（`compat/sodium/SodiumCompat`，纯反射；第一版误写成「装了 Sodium 就硬拒」——Fabric 上 Iris 硬依赖 Sodium，那等于把功能永久锁死，已拆）。Voxy 第二渲染栈经维护者裁定后**也已同批搬入**（`compat/voxy` 两件 + `mixin/client/voxy` 三件 + `tacz.voxy.mixins.json` + `allChanged` 两道闸；本线 AP 不能关 ⇒ 可选-mod mixin 用 **@Pseudo** 过编译门，不是加依赖也不是排除包；Voxy 侧 475 行深反射的类名布局取自 0.2.19，本 MC 线上是否对得上**未核**，对不上则安静降级为"镜内没 LOD"）。解锁条件写在 `docs/lineage/SCOPE_PIP_SHADER_ISOLATION_PORT_2612_20260901.md` §3。**顺序偏离记账**：`docs/SCOPE_PIP_RERENDER_1211_PORT_PLAN_20260830.md` 原把这些键列为"不做、等 B5"，并写明光影下二次渲染的前置是 **B5 离屏重定向** ⇒ 本批提前接了四条键但**没做 B5**，靠三处默认不触发的闸 + 日志自检兜；实机若见镜内是上一帧/黑屏，正解是补 B5 而不是继续叠隔离补丁。（本行前一版曾写"订正 L-14 所称配置键已接线"——那是**我的误记**，我方账本 L-14 讲的是姊妹 NeoForge 线 text_show，与本条无关；已按实读撤回，隔离键缺失的结论改以我方 `RenderConfig.java` 的 grep 为据。）软注入命中与否沙箱不可验 ⇒ 已配日志回执，判别法见方案篇 §5 |
| L-19 | 实机"镜内外均 1X"：放行窄遍的两道同批守卫漏搬 | 26.1.2 `825d2c5`+`95590b0` → 1.21.11 | **已修待实机复测**（CI `success`） | 维护者实机发现、并要求全量对照 ⇒ 我方"放行光影窄遍"只搬放行、漏了 `captureSceneAfterIrisFinal` 的 `rerenderMode()` 早退（宽视场成品覆盖窄视场 ⇒ 镜内 1x）与 `IrisFinalScopeOverlayMixin` 的窄遍早退+`discardPendingOverlays()`（一帧两次 finalize ⇒ 合成回灌自身、镜内定格）；另补 `shaderRerenderAllowed()` 公有闸与 `ScopePipRerenderInterval`+`sceneTargetGeneration`。**方法论入册**：逐 commit cherry-pick 抓不到"嵌在既有方法体里的守卫"，必须跑 `scripts/audit_sibling_render_line.py` 的终态 diff 层（补完后相关文件 0 行同形）。CI 全绿≠功能对，教训同见 `docs/lineage/AUDIT_2612_RENDER_42COMMITS_20260901.md`；另**主动加固一处**（他们无）：`worldZoomForcedToOne()` 改为只在窄遍真会跑时压 1×，否则退回重投影/整屏变焦——避免"被拒时内外一起 1X 且不自愈" |
| L-8 | 光影下「枪身挡住太阳/月亮那块继承天体自发光」：A/B/C 判别已跑完 —— A（`MeshPolyInShadow`）**无效**、B（关掉 `MeshGpuUnderShaders` + `MeshGpuWorldUnderShaders`）**有效**，第一/第三人称与展示台一致 ⇒ 根因在我们自开的 GPU pass；本分支已把这两键退回 false，并修掉 `resolveLightmap` 的一次性闩锁（光影下取不到 lightmap 改为整条拒收，不再降级到带 `EMISSIVE` define 的管线） | 1.21.11 → 26.2（`arena/01a04e96`）**与** 26.1.2（`arena/01a05170`） | **PASS（两键默认关的语境，2026-08-31 维护者复测）/ OPEN（代码修法本身仍待带光影开键自证）** | 26.2 那边形态相同：`MeshPolyInShadow` 同样默认 false、`resolveLightmap` 同款闩锁、且「光影下走 GPU」是默认开 ⇒ 现象应当更明显。要求回给：① 老日志里搜 `lightmap` —— 那行 WARN 出现过 ⇒ A12 就是成因，照抄两条修法即可；从没出现过 ⇒ 未排除的是「自建管线 MRT / color target 集合与 `ENTITY_CUTOUT` 不一致」。26.1.2 别把这两个键当 true 移植（`SYNC_GUIDE_1211_TO_2612_TML_GPU_20260831.md` §1.4）。判别与修法全文：`docs/MESH_LOADER.md` §5.9 / §5.10、`docs/REVIEW_UPSTREAM_TML_GPU_262_20260831.md` A11（已否证）/ A12 |
| L-5 | 1.21.11 孤儿 mixin 配置 `tacz.compat.acceleratedrendering.mixins.json` | 本分支 | **DONE** | 新增的「mixin 配置注册性」静态校验当场抓出：该 json 无对应 mixin/plugin 类、且不在任何 `fabric.mod.json` 的 `mixins` 数组里（本分支 AR 兼容走 `ARCompat` 空壳，理由写在该类注释）。已删该文件。26.2 侧同名配置**有**对应类（`BedrockPartMixin` 存在），所以这条只适用于 1.21.x 纪元 |
| L-23 | 26.2 R3 末轮两件同病同步：`tacz:nbt` 跨包材料（`61345c58`）+ 开镜 mesh 距离闸门按角尺寸补偿（`08869095`） | 26.2 `arena/01a04e96` → 1.21.11 | **已落地待 CI/实机**（CI 编译门待跑；沙箱无 JDK，本地不可编译。未做实机验证） | 逐条对照 26.2 tip `08869095`：其余同病项已在 L-15/L-16/L-18/L-10 落地，本次剩两条。① `TaczNbtIngredient` 新增并注册（partial 布尔：true=`CustomData.matchedBy`、false=tag 全等）；`normalizeLegacyIngredientJson` 支持 `tacz:nbt` + Upgrader 的 `items` 字符串→数组 + 旧 `{item+nbt}` 无 type 改写 `forge:partial_nbt`；**接口适配**：本线 `getMatchingItems/toDisplay/getPacketCodec`，26.2 是 `items/display/getStreamCodec`，不能照抄；**有意保留** `forge:nbt` strict ItemStack 归一化（26.2 末版折叠掉）。② `ScopePipRenderState.currentDetailZoom()` + `PolyRenderPolicy` 两道闸门乘补偿系数、Throwable 兜底回裸眼语义。26.2 侧回应：他们同批已做 mesh ocular clip/FCAP bridge/世界消费点，前两者本线已有或已按世代边界判不搬（见 L-2/L-12/L-18/L-21/L-22），不属于本次。验收点在 CHANGELOG 顶部「与 26.2 R3 末轮同步」 |
| L-24 | 「重启后配置回默认」的可见出口：ModMenu 入口此前没接 `setSavingRunnable` | 26.2 `MenuIntegration` 形状 → 1.21.11 | **已落地待 CI/实机** | 根因：本线 L-21/L-22 的 `setSavingRunnable(ConfigPersist::saveAll)` 只放在 `getConfigScreen()`；ModMenu（`ModMenuApiImpl`）直接 `getConfigBuilder().setParentScreen(parent).build()` ⇒ 绕开保存链 ⇒ ModMenu 里改配置保存只改内存、重启回默认。修法对齐 26.2：把 hook 移进 `getConfigBuilder()`（所有入口共用一份 builder）。注：26.2 用 `ModConfig.getLoadedConfig().save()`（其 FCAP 26.2.x 公开），本线 FCAP 21.11.1 的 `LoadedConfig` 包私有，保留 Accessor+TomlWriter（L-21）不照抄。验收点见 CHANGELOG 顶部「重启后配置回默认的最后一个出口」 |
| L-25 | 镜内裁手：`ScopeRenderTypes.armClipped(skin)` + `RenderHelper#wrapForScopeClip` collector 代理（26.2 `armClipped` 等价物） | 26.2 `ScopeBodyRenderTypes`/`RenderHelper` → 1.21.11（深度孔径形状） | **已落地待 CI/实机**（沙箱无 JDK，本地不可编译；实机未验） | 本线此前只有枪身/火光 masked 类型，无手臂 masked 类型；因 `AvatarRenderer#renderHand` 内部自选 `entityTranslucent(skin)`，改以 `SubmitNodeCollector` Proxy 在提交穿过时 identity 替换（同 26.2 机制）。管线复用 `FLASH_TRANSLUCENT_PIPELINE`（=`entityTranslucent` 镜像 + `scope_flash_clip.fsh` MASK_OUTSIDE），RenderSetup 含 `affectsCrumbling/sortOnUpload/outline`，单独 `ARM_TRANSLUCENT_TYPES` 缓存；门禁 `ScopeRenderTypes.hasScheduledViewmodelAperture()` + 运行期 mode-0 fail-open。覆盖普通开镜、PIP 重投影、PIP 窄遍三格 × 光影开/关（主手部 pass 同一 aperture）。验收与脚本见 CHANGELOG 顶部「镜内裁手」。

## 3. 版本号状态

## 3. 版本号状态

| 分支 | mod_version | README 一致性 |
|---|---|---|
| 26.2（`arena/01a04e96`） | `1.1.8+fabric.26.2.R3` | 由他们自己回填 |
| **1.21.11（本分支）** | `1.1.8+fabric.1.21.11.R3` | ✅ `bash scripts/check_release_consistency.sh --strict` 通过（6 ok / 0 fail / 1 warn：arena 分支名不是 MC 系列，属预期跳过） |
| 26.1.2（`arena/01a05170`） | `1.1.8+fabric.26.1.2.R2-hotfix2` | 待他们 bump |

> 本分支 R3 = 把 `-hotfix2` 后缀去掉、直接进 R3，与 26.2 侧 `5bb13af` 的做法一致
> （规矩见 `gradle.properties` 注释与 `check_release_consistency.sh` 头部：序号直接接在
> `R<n>` 后，`-hotfix<n>` 只在真正发补丁时用）。
