# 26.1.2 线同步轮：对表 26.2（几近完成）与 1.21.11（2026-09-02）

> **本文是 26.1.2 线这一轮的同步主记录**，与 26.2 线的 `SYNC_ROUNDUP_R3_20260831.md` 同一体例
> （那是家族级索引、本线副本见本目录同名文件）。
> 触发：维护者「26.2 的分支已经几近完成了，你对照一下你们俩的进度，然后进行同步」。
> 取货范围（维护者 2026-09-02 圈定）：**26.2 + 1.21.11 都搬，另含 FCAP 配置桥；渲染项一起做；
> 并把 1.21.11 的请回项写进同步记录**。

日期 2026-09-02。执行分支 `arena/01a05db3…`（承 `arena/01a05170…`，PR #83 → `26.1.2`）。
取样口径：全部 `git ls-remote` / `gh api` 远端实拉 + 终态文件逐行 diff，**不采信任何一方的自述**。

---

## 0. 三条线进度底账（2026-09-02 远端实拉）

| 线 | 分支 | 尖端 | 领先基线 | 状态一句话 |
|---|---|---|---|---|
| **26.2 工作线** | `arena/01a04e96` | `01698440`（末个真提交 `08869095`，09-01 15:29） | 领先 `26.2(main)` 84 提交 / **44 个真提交** | **几近完成**：TML 整线、mask 纪元 PIP、`tacz:nbt` 合成、FCAP 配置桥、CI v4 三件套、`docs/lineage` 目录制；末轮在做配置默认值定稿与实机返修 |
| **26.1.2（本线）** | `arena/01a05db3` ← `01a05170` | `f8f5ed9c`（本轮） | 领先 `26.1.2` 128 提交 / **81 个真提交** | final-overlay 回搬 + 检视动画两连修 + CI + **TML 整线（取自 1.21.11）** + **rerender PIP** + mesh 目镜裁剪 + 配置落盘修复；本轮再收 26.2/1211 六项 |
| **1.21.11** | `arena/01a05759` | `f53dd13b`（末个真提交 `8b4355a8`，09-01 14:32） | 领先 `1.21.11` 224 提交 / **123 个真提交** | 已把 26.1.2 渲染线整套搬过去（42 提交逐条甄别），另有自己的配置落盘三处补齐、光影隔离大件、Voxy/Sodium 同批 |

**拓扑要点**（决定谁能给谁什么）：
- 26.1.2 与 1.21.11 **同纪元**（`SubmitNodeCollector` 世代、无 `RenderType.prepare()`），
  26.2 是下一代（有 `prepare()` / `PreparedFrame#executeSolid`）；跨线搬的是**机制与判定**，不是代码。
- 本线的 PIP 是**深度孔径 + 二次渲染**架构，26.2 是**掩码/合成**架构 ⇒
  26.2 的 `ScopePipRenderer` / `ScopeMask*` / `ScopeBodyRenderTypes` 一族在本线**没有对应物**，不可整搬。
- 本线的 TML 整线来源**是 1.21.11 而不是 26.2**（`SYNC_ROUNDUP_R3` §1 主线一钦定），
  所以 26.2 的 TML 提交里只有「我们还没有的那些增量」才是货。

---

## 1. 对照方法

1. `git ls-remote` + `gh api` 拉齐三方尖端；
2. 对 26.2 线改过的每个文件（84 提交、113 个文件）逐个判：我方有且逐字相同 / 有但不同形 / 我方根本没有；
3. 「有但不同形」的再分：版本世代差异（不搬）、架构差异（不搬）、**我方真的缺东西**（搬）；
4. 配置面另跑一次键级 diff（`MeshyConfig` / `RenderConfig` 的 `define("…")` 全表对照）；
5. 1.21.11 侧按它们 09-01 写给我们 26.1.2 的两份文档（`SYNC_CHECKLIST_1211_TO_2612_PIP_20260901.md`、
   `AUDIT_2612_RENDER_42COMMITS_20260901.md`）+ 逐 commit 正文双向核对。

---

## 2. 26.2 → 26.1.2 处置表

### 2.1 本轮已搬（代码）

| 26.2 提交 | 内容 | 本线处置 | 证据级别 |
|---|---|---|---|
| `61345c5` | 注册上游 1.21.1+ 的 `tacz:nbt` 自定义材料（跨包合成真凶） | **已搬**（`TaczNbtIngredient` 新文件 + `TaCZFabric` 注册 + 归一化支持该类型） | 机制=26.2 实机日志；本线=同形移植、编译门过、**实机未验** |
| `7227ff9`（合成部分） | `GunSmithTableIngredient`：旧式不带 type 的 `{item+nbt}` 改写为 `forge:partial_nbt`；`catch` 加 `LinkageError`；失败日志带规范化形态 | **已搬**（四处全搬） | 同上 |
| `61345c5`（归一化） | `items` 写成单个字符串时包成单元素数组（TaCZPackUpgrader 形态） | **已搬** | 同上 |
| `7227ff9`（配置部分） | FCAP 落盘桥：`ModConfig#getLoadedConfig()#save()` | **已搬（形状不同）**：v26.1.5 的可见性未经证实 ⇒ 本线用**反射探测**，命中走 FCAP 官方路径、没命中退回 Accessor 显式写回，两条各打一条 INFO | 26.2=源码实读；本线反射分支=**未验**（FCAP 26.1.5 上是否命中由实机那条 INFO 回答） |
| `7227ff9`（时序） | `ModConfigEvents` 回调必须在任何 `ConfigRegistry.register` 之前挂（FCAP 在 register 里当场加载配置） | **已搬**（含 pre 那份最先注册、晚挂正好漏掉它） | 机制=26.2 源码实读；本线=同形移植、**未验** |
| A6（下游审查采纳） | 每帧烘焙额度与 LRU 容量**解耦**（独立键 `MeshGpuBakeBudgetPerFrame`） | **已搬**：新增键（默认 4，1-64）+ `tryReserveBake()` 改读新键 + log-once + Cloth 条目 + 中英语言键。本线原先是 `Math.max(4, cap)`，一个旋钮当两个用 | 机制=26.2 审查；本线=同形移植、**未验**（默认 4 与旧行为一致，属保守） |
| `0886909` | meshloader 两道距离闸门乘上**当前开镜倍率**（镜内高模枪恒为立方体） | **已搬**：`PolyRenderPolicy` 两处 `distance *= detailZoom()`；倍率源是本线自己的 `ScopePipRenderState#currentDetailZoom()`（本线无 `ScopePipRenderer`，公式与 26.2 同式 `1+(Z-1)·progress`） | 机制=26.2 实机回报；本线=同形移植、**实机未验** |
| `docs/ci/` 三件套 | `build.yml`（全量 build + jar artifact + 三个静态校验）、`consistency.yml` v2、`compile-check.yml` v4 | **已搬为暂存区**（`docs/ci/`，按 AGENTS.md §1 记载的 workflows 权限限制与 26.2 的暂存区惯例，Agent 不碰 `.github/workflows/`）；另把 `scripts/check_release_consistency.sh` 从 `26.2(main)` 镜像进本分支（两个 workflow 都要跑它，CI 跑在工作区，本分支必须有这份文件） | 静态：本分支工作区跑过一致性脚本 → 通过 6 / 失败 0 |
| `docs/lineage/` 目录制 | 账本 + 三份同步指导 + 家族树 + 治理提案 + R3 总纲 | **已搬**：家族级四份按副本引入（顶部注明「副本、勿在副本里另立一套」），另建本文与本线账本 | — |

### 2.2 不适用（结构/世代不同，明确不搬）

| 26.2 项 | 为什么不适用 |
|---|---|
| `ScopePipRenderer` / `ScopeMaskRenderer` / `ScopeMaskTarget` / `ScopeMaskGeometry` / `IrisScopeMaskState` / `ScopeBodyRenderTypes` / `ScopeTextRenderTypes` / `scope_text.fsh|vsh` | 26.2 是**掩码/合成**纪元；本线是**深度孔径 + 二次渲染**。本线对应物是 `ScopePipRenderState` / `ScopePipRerender` / `ScopeDepthCopyState` / `ScopeRenderTypes`，没有可映射的掩码目标 |
| `PreparedFrameSolidMixin` / `LevelRendererWorldPassMixin` / `LevelExtractorScopePassMixin` | 26.2 的 `prepare()` / `PreparedFrame#executeSolid` 世代入口，本线无此 API |
| `RenderConfig` 里 26.2 独有的 29 个键（`AimingSwayIntensity` / `HandViewLockFix` / `IrisHandPhaseSplitFix` / `ScopeMask*` / `ScopeSightClipFix` / `Laser|Tracer|Shell*Debug` …） | 逐键核对：要么绑在 26.2 的掩码/手部相位分支上，要么是 26.2 自己的调试件。本线没有对应实现，搬了就是死开关 |
| `99b15b2`（三件来自本线的修复） | 方向反了：那是 26.2 从**本线**搬的（纹理 pass 内懒加载、Voxy 预热窗口改绑、两处 tooltip 的 `I18n.get`）。本线本来就有 |
| `bb6fcb6`（A1-A10 审查处置）中属于 26.2 自证的几项 | 本线的 TML 来自 1.21.11 的终态，那批审查的多数结论已在来源里；剩下的是 26.2 用自己的 Iris 源码证伪的 A4/A9，与本线无关 |

### 2.3 观望（等 26.2 实机收敛后再评估）

| 26.2 项 | 观望理由 |
|---|---|
| `IlluminatedLights`（`bb6fcb6` 新增，53 行） | 本线用 `MeshPolyIlluminatedRealSky`（`MeshyConfig`）表达同一诉求，且 26.2 把它放在 `RenderConfig`。**两者是不是同一机制的两种形态未核**，本轮不动 |
| `98298fa`（R3 配置默认值定稿） | 本线两键 `MeshGpuUnderShaders` / `MeshGpuWorldUnderShaders` **默认开**（与 26.2 R3 定稿一致，理由写在 `MeshyConfig` 注释）；1.21.11 是**默认关**并要我们的实测数据 —— 这条是三方唯一还开着的判定分歧，见 §4 |

---

## 3. 1.21.11 → 26.1.2 处置表

| 来源 | 内容 | 本线处置 | 证据级别 |
|---|---|---|---|
| `cd14a2ac` | **配置落盘三处补齐**：① pre 那份 spec（`tacz-pre.toml` / `DefaultPackDebug`）纳入落盘表；② 取不到内存配置时不再静默 return，改 WARN；③ 改合并写 + 同目录临时文件 `ATOMIC_MOVE`（原实现先截断目标文件，掉电即毁配置） | **全部已搬**：`ConfigPersist` 落盘表改按文件登记（`recordNamed`），`PreLoadConfig` 改用它注册；`OverwriteCommand` 改完显式落盘（它绕过面板的 `savingRunnable`） | 1.21.11=实机测出；本线=同形移植、**未验** |
| `837924b3` | `worldZoomForcedToOne()` 加 `scopePassRunnable()` 判据：镜内那遍被拒时不再把内外一起压成 1X（1.21.11 注明「26.1.2 无此项」） | **已搬**：新增 `scopePassRunnable()`（无光影恒真；有光影看 `ScopePipRenderState.shaderRerenderAllowed()`） | 1.21.11=实机；本线=同形移植、**未验** |
| `SYNC_CHECKLIST` §2-1 | 镜内文字裁剪加一条 log-once 判据（否则「走了掩码」与「回退 vanilla」在屏幕上长得一样） | **已采纳**：`ScopeTextSubmitter` 提交成功时打 `[TACZ Scope] In-scope text is now clipped to the ocular aperture mask (N font page group(s)).`，只打一次 | 采纳（成本极小，无行为风险） |
| `SYNC_CHECKLIST` §2-2 | 字体页缓存遇到资源重载的剧本 | **登记未做**：清空入口要接在客户端资源重载监听上（本线现成入口是 `TaczMeshyIntegration` 那种 `registerReloadListener`），本轮不做一个没验过的运行期改动去换一个还没人踩到的隐患；隐患写进 `ScopeTextSubmitter` 的 javadoc | 立项 |
| `SYNC_CHECKLIST` §2-3 / §2-4 | 反向失败模式（fsh 在 `tacz_ScopeFinalOverlay == 0` 时丢弃全部像素）要写进验收矩阵；`PageHandle.texture` 非必需 | §2-3 写进本文 §5 的验收矩阵；§2-4 仅登记（本线那行 `this.texture = view.texture()` 在 26.1.2 可用，不必改） | 文档 |

---

## 4. 反向：本线 → 两条线

- **已经流出去的**：26.2 `99b15b2` 从本线搬了三件（纹理 pass 内懒加载 / Voxy 预热窗口改绑 / 两处 tooltip 的 `I18n.get`）；
  1.21.11 `8d28e57` + `04869be` 把本线的渲染线（G1 mesh 目镜裁剪、G2 掩码帧戳、G3 手部坐过窄遍、G4 PIP 阈值/渐变）整套搬走并做了 42 提交甄别。
- **本轮新产出、可能值得流出去的**：
  1. `tacz:nbt` 的**归一化三段**（items 字符串→数组、无 type 的 `{item+nbt}`→partial、LinkageError+规范化日志）—— 1.21.11 线也**没有** `TaczNbtIngredient`（已核实其 `util/forge` 下只有 Partial/Strict 两件），本线这一份是现成的第三份货；
  2. `ConfigPersist` 的**双路径形状**（FCAP 官方路径反射探测 + 合并原子写回退）—— 对 1.21.11（FCAP 21.11.1 同样未做 javap 核实）与 26.2 都成立；
  3. `worldZoomForcedToOne` 的 `scopePassRunnable` 判据 1.21.11 已有、26.2 无。
- **仍开着的判定分歧**（三方唯一）：光影下 `MeshGpuUnderShaders` / `MeshGpuWorldUnderShaders` 的默认值。
  26.2 R3 定稿与本线 = **默认开**（理由：常驻 VBO 的收益胜过「枪身挡住太阳月亮会继承天体亮度」这个可观测、可整键关闭的取舍）；
  1.21.11 = **默认关**并要我们的实测数据。**本轮无法回答**：本沙箱无运行环境、无光影环境，给不出帧时间/现象数据 —— 见 §5 待办，不编数据。

---

## 5. 本轮落地清单与验收

**代码（commit `f8f5ed9c` + 本轮第二批）**
1. `tacz:nbt` 自定义材料（新增 `TaczNbtIngredient`；`TaCZFabric` 注册；`GunSmithTableIngredient` 四处）
2. `ConfigPersist` 重写（FCAP 双路径 + pre 纳入 + 不再静默 + 合并原子写）+ `PreLoadConfig` / `LoadingConfigEvent` / `TaCZFabric` 时序 / `OverwriteCommand`
3. `PolyRenderPolicy` 开镜倍率补偿 + `ScopePipRenderState#currentDetailZoom`
4. `ScopePipRerender#worldZoomForcedToOne` 加 `scopePassRunnable`
5. `MeshGpuBakeBudgetPerFrame` 独立键（A6 解耦）+ Cloth + 中英语言键
6. `ScopeTextSubmitter` 的 log-once 判据
7. `scripts/check_release_consistency.sh`（镜像）+ `docs/ci/` 三件暂存模板 + `docs/ci/README.md`

**编译门**：CI `compile-check` 对 `f8f5ed9c` **success**（`build-reports/compile-java.log`）。
第二批（烘焙额度键 / log-once）待 CI。

**实机验收清单（本线一行未验，全部待维护者）**

| # | 场景 | 期望 | 若不符 |
|---|---|---|---|
| 1 | 装一个被 TaCZPackUpgrader 升级过的枪包（配方里是 `tacz:nbt`） | 工作台材料格正常显示带 GunId 的枪、能合成（此前材料格空白） | 先看 latest.log 有没有 `Failed to resolve gun smith table ingredient …（带规范化形态）`；没有则是 serializer 没注册 |
| 2 | 面板随便改一个客户端配置 → 保存 → 重启 | 值保留；`config/tacz-client.toml` mtime 变化 | 搜 `[TACZ] Config persist:` 两条 INFO 之一，确认走的是 FCAP 官方路径还是回退路径；再看有没有 WARN |
| 3 | 面板改 `Other`（`DefaultPackDebug`）→ 保存 → 重启 | 保留 | pre 那份没登记的话会静默回默认（1.21.11 实测的断点） |
| 4 | 举 4x 镜看远处高模枪（掉落物 / 第三人称） | 镜内是烘焙后的模型，不是立方体 | `PolyRenderPolicy#detailZoom` 的兜底是 1.0（scope 线出任何问题都不会连坐 mesh），先确认开镜进度/倍率取值 |
| 5 | 光影下开镜（未开 opt-in → 镜内那遍被拒） | 世界不再被压成 1X，退回重投影/整屏变焦，有可用画面 | `worldZoomForcedToOne` 的判据没生效（这是本轮新加的） |
| 6 | 镜内文字 | latest.log 出现一次 `[TACZ Scope] In-scope text is now clipped …` | **反向失败模式（1.21.11 §2-3）**：文字整个消失时，第一嫌疑人是 fsh 在 `tacz_ScopeFinalOverlay == 0` 时**丢弃全部像素**（故意 fail-closed），查这次绘制是否真用了 `maskedText` 且被 `Operation.MASK` 包住，而不是「裁剪太狠」 |
| 7 | 几十把掉落枪横跨明暗边界 | 不再「逐出-重烘」打摆；日志出现一次 `World bake budget (4 per frame) exhausted` 属正常 | 调 `MeshGpuBakeBudgetPerFrame`（与 `MeshGpuLightCacheSize` 独立） |

---

## 6. 账本

见同目录 `HANDOFF_LEDGER.md`（本线副本 + 本轮新增行 M-1…M-8）。
