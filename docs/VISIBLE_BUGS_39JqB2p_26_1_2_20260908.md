# 玩家日志 39JqB2p 可见问题移植 — refab 26.1.2

日期：2026-09-08。基线：`3015e8b`。对应六线交接账本 #17。

**状态：静态修复、待实测。** 本件不解释、也不宣称修复「开光影世界全透明」。
仅处理本线适用的 A/B/C/D；E 的空手 scope mask WARN 在本线不存在。
版本保持 `1.1.8+fabric.26.1.2.R3`，不是发布。

## 落码范围

- **A**：`GunSmithTableRecipe.isSpecial()` 返回 `true`。本配方不参与 vanilla
  recipe-book placement，避免 `NOT_PLACEABLE` 导致逐条 empty ingredients WARN；
  `placementInfo()` 和枪械工作台自己的材料校验均不变。
- **B**：交互键实体白名单将已移除的 `minecraft:boat` / `minecraft:chest_boat`
  改为 `#minecraft:boat` 和十条可选 chest boat/raft 实体（含 pale oak、bamboo）；
  保留其他实体和 `replace:false`，避免缺失实体引用导致整条标签构建失败。
- **C**：新增 Glock 17 动画源码覆盖件，逐字节取自
  `1aca7c74f8fd14f64e6a348e235eb87dfdfa8534` 的修正版。
  bundle 内原文件 blob 为 `82de964cee92b6b5e12e612a619c91eb67f451a9`；
  仅删除五行。交接文所称 raise 段，在 JSON 中的实际键是
  `animations.draw.sound_effects`，无效 effect 为 `p24_pi_golf17_stockskel_raise`。
  动画时长、骨骼、其他动画音效不变。不修改 bundle。
  现有 `processResources` 的源码优先 + `DuplicatesStrategy.EXCLUDE` 提供覆盖机制。
- **D**：删除 `IrisCompat.assignCommonEntityPipelinesToHandIfNeeded()`、两个 flag、
  `RenderPipelines` import，以及 `GunItemRendererWrapper` 的唯一调用点和过时注释。
  vanilla 的七条 entity/item 管线交回 Iris 自己逐 draw 分派。
  **分支适配**：本线除了 `tacz:pipeline/scope_*`，还有自有 `tacz:pipeline/mesh_*`
  的 HAND/ENTITIES 注册；这些不是 vanilla 管线，保留原状，不能按 26.2 的简写
  「只留 scope」误删。本件不改 mesh GPU 开关或其他渲染路径。

当前 checkout 及本次读取的远端 main 未含交接所列 2026-09-08 补丁文件，因此
A/B/D 按本线源码手工落实，未声称执行过该补丁的 `git apply --check`。
没有带入 `IrisShaderCreatorMixin` 开关或 `void main` 收紧。

## 验证

本地没有 Java，Java 编译与全量构建使用本会话 Arena 分支的 GitHub Actions。

- [x] `git diff --check`
- [x] `bash scripts/check_release_consistency.sh --strict`（6 通过；Arena 分支名非 MC
  版本形态产生一个预期警告）
- [x] `python3 docs/check_mixin_registration.py`
- [x] `python3 docs/check_lang_keys.py`
- [x] `python3 scripts/check_visible_bug_resources.py`：白名单条目完整、十种 chest
  boat/raft 可选、旧引用不存在；Glock JSON 与 bundle 仅差 draw 音效删除。
- [ ] CI `compile-check`（待本次推送后记录）
- [ ] CI `build`（待本次推送后记录）
- [ ] 产物资源覆盖检查：`python3 scripts/check_visible_bug_resources.py --jar <构建.jar>`

## 未执行的实机矩阵

1. 开光影，检查第一人称抛壳、枪口火光和高模枪体仍受光影光照；日志不再有
   vanilla `minecraft:pipeline/entity_*` / `item_*` 的 perfect-match HAND WARN。
   自有 mesh 管线合法注册的日志不是本次要消除的 vanilla 误注册。
2. 进世界/重载不再有 `Couldn't load tag tacz:interact_key/whitelist`；持枪对村民、
   矿车、展示框及普通船/运输船使用交互键，确认行为。
3. 不再有本工作台配方的 `can't be placed due to empty ingredients`；工作台配方列表、
   足料/缺料合成、JEI/REI 工作台分类与原先一致。
4. 切换 Glock 17，确认不再出现 `Missing gun sound resource … golf17`，其他动画音效正常。

以上实机项全部待测；编译通过不代表实机验证通过。
